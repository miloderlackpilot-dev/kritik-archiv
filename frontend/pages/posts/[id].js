import { useEffect, useState } from 'react';
import { useRouter } from 'next/router';
import Link from 'next/link';
import axios from 'axios';
const API = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5000';
export default function PostPage() { const { query } = useRouter(); const [post, setPost] = useState(null); const [error, setError] = useState(''); useEffect(() => { if (!query.id) return; axios.get(`${API}/api/posts/${query.id}`).then((r) => setPost(r.data)).catch((e) => setError(e.response?.data?.error || 'Beitrag nicht gefunden')); }, [query.id]); if (error) return <main className="container"><p>{error}</p><Link href="/">← Archiv</Link></main>; if (!post) return <main className="container"><p>Lädt ...</p></main>; return <main className="container" style={{ maxWidth: 850 }}><p><Link href="/">← Zum Archiv</Link></p><article className="card"><h1>{post.title}</h1><p className="small">Ereignisdatum: {post.event_date || 'nicht angegeben'} · Einordnung: {post.claim_type} · Ordner: {post.folder_name}</p><div style={{ whiteSpace: 'pre-wrap', lineHeight: 1.6 }}>{post.content}</div><h2 style={{ marginTop: 28 }}>Quellen</h2>{post.sources.map((s) => <div key={s.id} style={{ borderTop: '1px solid #e5e7eb', padding: '12px 0' }}><a href={s.url} target="_blank" rel="noreferrer">{s.title || s.url}</a><div className="small">{s.source_type} · Lizenz: {s.license} · {s.attribution || 'Urheber nicht angegeben'}</div></div>)}</article></main>; }
