import { useState } from 'react';
import axios from 'axios';
import { useRouter } from 'next/router';
import Link from 'next/link';
const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:5000';
export default function RegisterPage(){const [form,setForm]=useState({email:'',username:'',password:''});const [message,setMessage]=useState('');const [error,setError]=useState('');const router=useRouter();async function submit(e){e.preventDefault();setMessage('');setError('');try{const r=await axios.post(`${API}/api/auth/register`,form);setMessage(r.data.message);setTimeout(()=>router.push('/login'),1800);}catch(err){setError(err.response?.data?.error||'Registrierung fehlgeschlagen');}}return <div className="container" style={{maxWidth:500}}><div className="card"><h2>Registrierung</h2>{error&&<div className="alert error">{error}</div>}{message&&<div className="alert success">{message}</div>}<form onSubmit={submit}><label>E-Mail<input className="input" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><div style={{marginTop:12}}><label>Username<input className="input" required value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></label></div><div style={{marginTop:12}}><label>Passwort<input type="password" minLength={8} required className="input" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label></div><button className="button" style={{marginTop:16,width:'100%'}} type="submit">Registrieren</button></form><p style={{marginTop:12}} className="small">Bereits registriert? <Link href="/login">Login</Link></p></div></div>;}
